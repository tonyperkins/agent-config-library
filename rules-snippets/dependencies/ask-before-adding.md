## Dependencies
- Don't add new dependencies without asking first — prefer stdlib and existing deps
- If a dependency is necessary, explain why current deps don't cover the use case
- Pin exact versions, not ranges — reproducible installs matter
- After adding a dep, run the test suite to confirm nothing broke
