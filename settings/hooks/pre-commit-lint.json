{
  "_comment": "Example hooks block for settings.json — runs lint after edits, before letting a commit-shaped action proceed.",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "npm run lint --silent || true"
          }
        ]
      }
    ]
  }
}
