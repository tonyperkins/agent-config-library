<!--
AGENTS.md — the cross-tool standard read by multiple coding agents (Codex,
Cursor, etc.), not just Claude. Keep it tool-agnostic; put Claude-Code-specific
things (skills, slash commands) in CLAUDE.md instead.
-->

# [Project name]

## Setup
- Install: `[command]`
- Run: `[command]`

## Build & test
- Build: `[command]`
- Test: `[command]`
- Lint/format: `[command]`

## Code conventions
- [Tool-agnostic style rules — language idioms, naming, file organization]

## PR / commit expectations
- [What a "done" change looks like before it's handed back]

## Notes for agents
- [Anything an autonomous agent specifically needs to know that a human contributor
  would pick up by osmosis — e.g. "don't touch generated/ directory", "ask before
  adding new dependencies"]
