<!--
Minimal AGENTS.md template for cross-tool compatibility.
Keep this under 20 lines — it's the shared base that all AI coding tools read.
Put tool-specific config in CLAUDE.md, .cursor/rules/, .github/copilot-instructions.md, etc.
-->

# [Project name]

## Setup
- Install: `[command]`
- Run: `[command]`

## Build & test
- Build: `[command]`
- Test: `[command]`
- Lint: `[command]`

## Conventions
- [Tool-agnostic style rules — language idioms, naming, file organization]
- [Don't touch generated/ directories]
- [Ask before adding new dependencies]

## Done criteria
- Tests pass, lint is clean, no secrets in diff
